import * as userAPI from './user-api'

export async function createAccount(user) {
	console.log(user)
	// try {
	// 	return await userAPI.index()
	// } catch(err){
	//     console.log(err)
	//     throw new Error(err)
	// }
}
